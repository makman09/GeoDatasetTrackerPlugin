services:
  backend:
    build: ./backend
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./backend/data:/data
    environment:
      - DATABASE_URL=sqlite:////data/geo.db
      - RUN_MIGRATIONS_ON_START=1
    restart: unless-stopped
